const Header = () => {
    const template = `
        <header>My Parcel App</header>
        <p>This is a boilerplate for a simple JS workflow
        using parcel</p>
    `

    return template;
}

export default Header;